/* style.css */
body {
    background-color:  #f3E4DC;
    font-family: new times roman;
}

.story-container {
    background-color: white;
    margin: 20px auto;
    padding: 20px;
    width: 80%;
    max-width: 600px;
    border-radius: 10px;
    text-align: center;
}

button {
    background-color: #654321;
    color: white;
    padding: 10px 20px;
    margin: 10px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

button:hover {
    background-color: # white;
}

/* Hide all pages initially except page1 */
#page2, #page3, #page4, #page5 {
    display: none;
}
